move these files into any directory sharing a name
with a subreddit and run
python reddit_bot.py

the script will scrape the sub for the top n posts
over the last day (configurable in config file)
and place the links into a text file which
is shown in index.php
